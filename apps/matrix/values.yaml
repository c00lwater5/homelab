elementweb:
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      external-dns.alpha.kubernetes.io/target: "homelab-tunnel.mukhtaar.org"
      external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    hosts:
      - host: &frontend_host chat.mukhtaar.org
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: element-tls-certificate
        hosts:
          - *frontend_host
  config:
    default:
      base_url: https://matrix.mukhtaar.org
      server_name: mukhtaar.org

dendrite:
  dendrite_config:
    global:
      server_name: matrix.mukhtaar.org
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hostName: matrix.mukhtaar.org
    tls:
      - hosts:
          - matrix.mukhtaar.org
        secretName: matrix-tls-certificate
  postgresql:
    enabled: true
